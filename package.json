{
  "name": "asm-emulator",
  "private": true,
  "version": "1.0.0",
  "description": "Веб-эмулятор ассемблера для лабораторных работ ( educational, client-side only )",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "tsc -p tsconfig.test.json && node -e \"require('fs').writeFileSync('.test-out/package.json','{\\\"type\\\":\\\"commonjs\\\"}')\" && node scripts/smoke-test.cjs"
  },
  "devDependencies": {
    "@types/node": "^25.7.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  }
}
